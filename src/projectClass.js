export default class Project {
    constructor(title, description, dueDate, priority, status, tasks, notes) {
        this.title = title;
        this.description = description;
        this.dueDate = dueDate;
        this.priority = priority;
        this.status = status;
        this.tasks = tasks;
        this.notes = notes;
    }

    addTask() {
        
    }
    /* 

    add functions to add tasks, change priority

    */
    
}
